## Landmatrix: CI (behave) Dockerfile


This repository contains the **Dockerfile** for [landmatrix-behave](https://hub.docker.com/r/sebastianmanger/landmatrix-behave/)
The container is only used for CI.
